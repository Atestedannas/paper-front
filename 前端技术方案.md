# 论文格式检查网站前端技术方案

## 1. 项目初始化

### 1.1 创建Vue3项目

使用Vite创建Vue3项目：

```bash
npm create vite@latest frontend -- --template vue
```

### 1.2 安装核心依赖

```bash
cd frontend
npm install
npm install pinia vue-router@4 element-plus axios pdfjs-dist docx-preview
```

### 1.3 项目配置

#### 1.3.1 Vite配置 (vite.config.js)

```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8002',
        changeOrigin: true
      }
    }
  }
})
```

#### 1.3.2 全局样式配置 (src/styles/index.scss)

```scss
/* 重置样式 */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* 全局变量 */
:root {
  --primary-color: #409eff;
  --success-color: #67c23a;
  --warning-color: #e6a23c;
  --danger-color: #f56c6c;
  --info-color: #909399;
  --text-primary: #303133;
  --text-regular: #606266;
  --text-secondary: #909399;
  --text-placeholder: #c0c4cc;
  --border-color: #dcdfe6;
  --border-light: #e4e7ed;
  --background-color: #f5f7fa;
}

body {
  font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', '微软雅黑', Arial, sans-serif;
  font-size: 14px;
  color: var(--text-primary);
  background-color: var(--background-color);
}
```

#### 1.3.3 入口文件配置 (src/main.js)

```javascript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import App from './App.vue'
import router from './router'
import './styles/index.scss'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.use(router)
app.use(ElementPlus)
app.mount('#app')
```

## 2. 项目结构

```
src/
├── api/                    # API请求配置
│   ├── index.js            # API入口
│   ├── auth.js             # 认证相关API
│   ├── paper.js            # 论文相关API
│   └── format.js           # 格式检查相关API
├── assets/                 # 静态资源
│   ├── images/             # 图片资源
│   └── icons/              # 图标资源
├── components/             # 通用组件
│   ├── PaperUpload.vue     # 论文上传组件
│   ├── FormatChecker.vue   # 格式检查组件
│   ├── FormatCompare.vue   # 格式对比组件
│   ├── CorrectionList.vue  # 修正建议列表组件
│   └── Layout.vue          # 布局组件
├── layouts/                # 页面布局
│   └── MainLayout.vue      # 主布局
├── pages/                  # 页面组件
│   ├── Home.vue            # 首页
│   ├── Login.vue           # 登录页
│   ├── Register.vue        # 注册页
│   ├── Dashboard.vue       # 仪表板
│   ├── Upload.vue          # 上传页面
│   ├── CheckResult.vue     # 检查结果页面
│   └── History.vue         # 历史记录页面
├── router/                 # 路由配置
│   └── index.js            # 路由入口
├── stores/                 # 状态管理
│   ├── user.js             # 用户状态
│   ├── paper.js            # 论文状态
│   └── format.js           # 格式检查状态
├── utils/                  # 工具函数
│   ├── request.js          # Axios封装
│   ├── file.js             # 文件处理工具
│   └── format.js           # 格式处理工具
├── App.vue                 # 根组件
└── main.js                 # 入口文件
```

## 3. 路由设计

```javascript
// src/router/index.js
import { createRouter, createWebHistory } from 'vue-router'
import MainLayout from '../layouts/MainLayout.vue'
import Home from '../pages/Home.vue'
import Login from '../pages/Login.vue'
import Register from '../pages/Register.vue'
import Dashboard from '../pages/Dashboard.vue'
import Upload from '../pages/Upload.vue'
import CheckResult from '../pages/CheckResult.vue'
import History from '../pages/History.vue'

const routes = [
  {
    path: '/',
    name: 'Home',
    component: Home
  },
  {
    path: '/login',
    name: 'Login',
    component: Login
  },
  {
    path: '/register',
    name: 'Register',
    component: Register
  },
  {
    path: '/dashboard',
    name: 'Dashboard',
    component: MainLayout,
    children: [
      {
        path: '',
        component: Dashboard
      },
      {
        path: 'upload',
        name: 'Upload',
        component: Upload
      },
      {
        path: 'result/:id',
        name: 'CheckResult',
        component: CheckResult
      },
      {
        path: 'history',
        name: 'History',
        component: History
      }
    ]
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  if (to.path.startsWith('/dashboard') && !token) {
    next('/login')
  } else {
    next()
  }
})

export default router
```

## 4. 核心组件实现

### 4.1 论文上传组件 (PaperUpload.vue)
